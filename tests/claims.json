{
  "$comment": "Claim -> test map enforced by @async/claims. Every anchor must appear verbatim in its source file, every listed test title must exist in tests/**/*.test.js, and every test titled 'PROMISE: ...' must be registered here.",
  "claims": [
    {
      "id": "readme.recursive-nodes-overlay",
      "source": "README.md",
      "anchor": "V1 nodes are the workflow-structure overlay",
      "tests": [
        "PROMISE: recursive nodes stay an execution overlay linked by ids"
      ]
    },
    {
      "id": "readme.event-driven-idle",
      "source": "README.md",
      "anchor": "The root should not loop or invent a\nsleep.",
      "tests": [
        "PROMISE: idle scheduler stops polling until human wake"
      ]
    },
    {
      "id": "readme.parallel-test-code-lanes",
      "source": "README.md",
      "anchor": "Tests and implementation are a good parallel case when their ownership is clean.",
      "tests": [
        "PROMISE: async pipeline schedules test and code lanes in parallel"
      ]
    }
  ]
}
